name: Build, Lint and Push Multiarch Docker Images

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & Static Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run ShellCheck
        run: |
          sudo apt-get update && sudo apt-get install -y shellcheck
          shellcheck -s bash \
            bootstrap \
            valheim-bootstrap \
            valheim-server \
            valheim-updater \
            valheim-backup \
            valheim-is-idle \
            valheim-tests \
            valheim-plus-updater \
            bepinex-updater \
            common \
            defaults \
            valheim-arch-diagnostics \
            steamcmd-wrapper \
            valheim-wrapper \
            tests/oracle-arm64-smoke-test.sh

      - name: Validate Docker Compose
        run: |
          docker compose -f docker-compose.yaml config >/dev/null
          docker compose -f docker-compose.oracle-arm64.yml config >/dev/null

  build-test-amd64:
    name: Build Docker Image for Testing (amd64)
    needs: lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image for testing
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          load: true
          push: false
          build-args: SOURCE_COMMIT=${{ github.sha }}
          tags: valheim-test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Save Docker image to tarball
        run: docker save -o valheim-test.tar valheim-test

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v7
        with:
          name: valheim-test-image
          path: valheim-test.tar
          retention-days: 1

  test-amd64:
    name: Run Test Matrix (amd64)
    needs: build-test-amd64
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        config:
          - name: default
            env: ""
          - name: valheim_plus
            env: "-e VALHEIM_PLUS=true"
          - name: bepinex
            env: "-e BEPINEX=true"
    steps:
      - name: Download Docker image artifact
        uses: actions/download-artifact@v8
        with:
          name: valheim-test-image

      - name: Load Docker image from tarball
        run: docker load -i valheim-test.tar

      - name: Run container with ${{ matrix.config.name }} settings
        run: |
          docker run -d --rm \
            --name valheim-test-${{ matrix.config.name }} \
            --cap-add=sys_nice \
            --stop-timeout 120 \
            -p 2456-2457:2456-2457/udp \
            -e SERVER_NAME="My Server" \
            -e WORLD_NAME="Neotopia" \
            -e SERVER_PASS=secret \
            ${{ matrix.config.env }} \
            valheim-test
          docker exec valheim-test-${{ matrix.config.name }} /usr/local/bin/valheim-tests "${{ matrix.config.name }}"

  build-arm64-check:
    name: Build ARM64 Image (Buildx Verification)
    needs: lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up QEMU for multiarch build
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build ARM64 image (verification only, no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/arm64
          push: false
          build-args: |
            SOURCE_COMMIT=${{ github.sha }}
            BOX64_TARGET=ARM64
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build_and_push:
    name: Build and Push Multiarch Docker Image
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    needs: [test-amd64, build-arm64-check]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=semver,pattern={{version}}
            type=sha
            type=raw,value=latest-arm64,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
          labels: |
            org.opencontainers.image.title=valheim-server
            org.opencontainers.image.description=Valheim Server Docker (Multiarch ARM64/AMD64)
            org.opencontainers.image.licenses=Apache-2.0
            org.opencontainers.image.source=https://github.com/${{ github.repository }}

      - name: Build and push multiarch Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: SOURCE_COMMIT=${{ github.sha }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          cache-from: type=gha

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          GENERATED_NOTES=$(gh api "repos/${{ github.repository }}/releases/generate-notes" -f tag_name="${{ github.ref_name }}" -q .body)
          printf '## Container Image\n```\ndocker pull ghcr.io/%s:%s\n```\n\n%s\n' "${{ github.repository }}" "${{ steps.meta.outputs.version }}" "$GENERATED_NOTES" > release-notes.md
          gh release create "${{ github.ref_name }}" --notes-file release-notes.md
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
